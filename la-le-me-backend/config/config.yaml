server:
  port: 8080
  mode: debug

database:
  host: localhost
  port: 5432
  user: la-le-me
  password: dev_password
  name: la-le-me

redis:
  host: localhost
  port: 6379
  password: ""

jwt:
  secret: dev_secret_change_in_prod
  expire_hours: 0.25

minio:
  endpoint: localhost:9000
  access_key: minioadmin
  secret_key: minioadmin
  bucket: la-le-me-backups
